=form_for @role do |f|
  - if @role.errors.any?
    - @role.errors.full_messages.each do |e|
      %div{:class => "alert alert-error"}
        =e
  =f.text_field :name
  =f.submit :value => "Add", :class => "btn btn-primary"